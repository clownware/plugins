Uppercase mono status pill. Tones: `neutral`, `ink`, `accent`, `ok`, `warn`, `err`. Add `dot` for a status indicator.

```jsx
<Badge tone="warn" dot>Low</Badge>
<Badge tone="accent">124 BPM</Badge>
```
