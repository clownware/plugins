Hairline text field with a cyan focus ring. Pass `mono` for technical values (BPM, catalog), `prefix`/`suffix` for adornments.

```jsx
<Input placeholder="Search pantry…" prefix={<SearchIcon/>} />
<Input mono suffix="BPM" defaultValue="124" />
<Input invalid placeholder="Required" />
```
