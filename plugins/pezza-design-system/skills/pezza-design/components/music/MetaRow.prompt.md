A single mono tracklist line: index, title/artist, and right-aligned BPM/key/duration. The metadata register that sells "producer".

```jsx
<MetaRow index={1} title="Subsurface" artist="Pezza" bpm={124} musicKey="A♭ min" duration="6:42" active />
```

Stack these inside a dark Card for a full tracklist.
