The signature Pezza waveform. Accent-cyan bars show played progress against a dim track. Deterministic from `seed`.

```jsx
<WaveBar progress={0.45} height={56} seed={12} />
```

Use on dark surfaces (now-playing, set scrubbers, story stickers). The accent fill is the one place cyan gets to glow.
